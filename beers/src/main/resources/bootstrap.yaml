spring:
  application:
    name: beers

server:
    port: 8000

eureka:
  instance:
    prefer-ip-address: true
    hostname: eureka
  client:
    serviceUrl:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE}